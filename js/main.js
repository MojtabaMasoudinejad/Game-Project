// addBackground(document.getElementById("app"));

// const startGame = new TextLoadGame(document.getElementById("app"));

// const gameEngine = new Engine(document.getElementById("app"));

// const keydownHandler = (event) => {
//   // event.code has different code for each key which can be used for using that keys of keyboard.
//   if (event.code === "ArrowLeft") {
//     gameEngine.player.moveLeft();
//   }

//   if (event.code === "ArrowRight") {
//     gameEngine.player.moveRight();
//   }
// };

// document.addEventListener("keydown", keydownHandler);
// // by pressing each key this eventListener will be activated

// gameEngine.gameLoop();
